import { createStore } from 'vuex'
import defaultBoard from '@/default-board'


export default createStore({
  state: {
    board:defaultBoard 
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
